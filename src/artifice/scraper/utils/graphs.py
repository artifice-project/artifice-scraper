# Custom monitoring functions for Monitoring Dashboard
# def size_of_contents(db):
#     from artifice.scraper.models import Content
#     sz = db.session.query(Content).count()
#     return sz

def size_of_foo():
    return 42
